---
title: 15. 三数之和
date: 2026-03-21 15:31:38
tags:
    - 算法
    - 指针
---

```
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        vector<vector<int>> res;
        sort(nums.begin(), nums.end());
        int n = nums.size();
        for (int i = 0; i < n; i++) {
            // 剪枝：当前数 > 0，不可能再有三数之和为0
            if (nums[i] > 0) break;
            // 去重：跳过重复的 瞄点，因为第一次算过了
            if (i > 0 && nums[i] == nums[i - 1]) continue;
            int left = i + 1;
            int right = n - 1;
            while (left < right) {
                int sum = nums[i] + nums[left] + nums[right];
                if (sum < 0) {
                    left++;
                } 
                else if (sum > 0) {
                    right--;
                } 
                else {
                    res.push_back({nums[i], nums[left], nums[right]});
                    // 去重 left
                    while (left < right && nums[left] == nums[left + 1]) left++;
                    // 去重 right
                    while (left < right && nums[right] == nums[right - 1]) right--;
                    left++;
                    right--;
                }
            }
        }
        return res;
    }
};


//我最初的想法是用map，因为找解快，但是去重很痛苦，顺序控制也没有。双指针找解快，去重也快，顺序也好。

//这道题的难点在于大量的去重。
```
