//#include <bits/stdc++.h>
#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Enter a number: ";
    cin >> n;
    // cin.ignore();  // 清掉上一行换行
    // string s;
    // getline(cin, s);    //这两个是绑定在一起的
    // string ans;
    // for(char c : s){
    //     if(c != ' ')
    //         ans += c;
    // }
    // reverse(ans.begin(), ans.end());
    cout << n << endl;


    return 0;
}